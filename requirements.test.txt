# Test dependencies for the Green Button Energy Import integration.
#
# Install with:
#   pip install -r requirements.test.txt
#
# pytest-homeassistant-custom-component (phcc) bundles its own pinned version
# of pytest-asyncio. Do NOT pin pytest-asyncio here — let phcc's dependency
# resolver pull the correct version (currently 1.3.0 for phcc >= 0.13.313).
# Requires Python >= 3.13.
pytest==8.3.5
pytest-homeassistant-custom-component>=0.13.313
pytest-cov==5.0.0