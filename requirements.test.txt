# Test dependencies for the Green Button Energy Import integration.
#
# Install with:
#   pip install -r requirements.test.txt
#
# pytest-homeassistant-custom-component (phcc) bundles pinned versions of
# pytest-asyncio and pytest-cov. Do NOT pin either of those here — let phcc's
# dependency resolver pull the exact versions it requires.
# Requires Python >= 3.13.
pytest==9.0.0
pytest-homeassistant-custom-component>=0.13.313
